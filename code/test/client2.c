#include "create.h"

int main(){
	int i, j;

	/*for (i = 0; i < NUM_LIASONS; ++i) {
		Exec("../test/liaison", sizeof("../test/liaison"));
	}*/

	for (i = 0; i < 1; ++i) {
		Exec("../test/liaison", sizeof("../test/liaison"));
	}

	/*for (i = 0; i < NUM_AIRLINES; ++i) {
		for (j = 0; j < NUM_CIS_PER_AIRLINE; ++j) {
			Exec("../test/checkinstaff", sizeof("../test/checkinstaff"));
		}
	}*/
}